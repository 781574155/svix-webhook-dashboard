server {
    listen       80;
    server_name  _;
    server_tokens off;

    location / {
        root   /work/html;
        index  index.html index.htm;
        add_header Cache-Control "no-cache";
        try_files $uri $uri/ /index.html;
    }
}